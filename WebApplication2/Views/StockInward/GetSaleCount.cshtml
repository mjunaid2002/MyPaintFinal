@if (Session["CurrentUserName"]?.ToString() == "Super Admin")
{
    <li style="background-color: #2C3E50 !important;" id="batch-status">
        <a href="@Url.Action("Indexstatus", "StockInward")">
            <i class="fa fa-list"></i> &nbsp;
            <span style="color:white">Purchase Requests</span>
            <span class="badge badge-light" id="SaleCount">0</span>
        </a>
    </li>
}
