@model CRM.Models.Brands
@{
    ViewBag.Title = "Creat";
}

<div class="content">
    <div class="content-header">
        <div class="leftside-content-header">
            <ul class="breadcrumbs">
                <li><a><i class="fa fa-cubes" aria-hidden="true"></i> Registration</a></li>
            </ul>
        </div>
    </div>
    <div class="row ">
        <div class="col-sm-12 col-md-12">
            <h4 class="section-subtitle"><b>Update Brands</b></h4>
            <a href="@Url.Action("Index","Brands")" class="btn btn-wide btn-success pull-right" style="margin-top:-40px"><i class="fa fa-list"></i> Brands List</a>
            <div class="panel">
                <div class="panel-content">
                    <div class="row">
                        <div class="col-md-12">

                            <form action="Edit" method="post" enctype="multipart/form-data" onsubmit="return confirm('Are you sure you want to Register ?');">
                                <div class="row">
                                    <div class="form-group col-sm-1" style="display:none">
                                        <label>ID</label>
                                        @Html.TextBoxFor(m => m.Id, new { @class = "form-control input-xs", @readonly = "readonly" })
                                    </div>
                                    <div class="form-group col-sm-3">
                                        <label>Brands Name</label>
                                        @Html.TextBoxFor(m => m.Name, new { @class = "form-control input-xs" })
                                    </div>
                                    @*<div class="form-group col-sm-3">
                                        <label>Start Date </label>
                                        @Html.TextBoxFor(m => m.StartDate, new { @class = "form-control input-xs", type = "date" })
                                    </div>
                                    <div class="form-group col-sm-3">
                                        <label>End Date</label>
                                        @Html.TextBoxFor(m => m.EndDate, new { @class = "form-control input-xs", type = "date" })
                                    </div>*@
                                    <div class="col-sm-12">
                                        <button type="submit" class="btn btn-primary" style="margin-top:10px"><i class="fa fa-plus"></i> Save </button>
                                    </div>
                                </div>
                            </form>

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


