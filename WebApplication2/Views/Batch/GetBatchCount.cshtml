@*@{
    int batchCount = ViewBag.BatchCount;
}

@if (Session["CurrentUserName"]?.ToString() == "Super Admin")
{
    <li style="background-color: #2C3E50 !important;">
        <a href="@Url.Action("Indexstatus", "Batch")">
            <i class="fa fa-list"></i> &nbsp;
            <span style="color:white">Batch Status</span>
            <span class="badge badge-light">@batchCount</span>
        </a>
    </li>
}*@

@if (Session["CurrentUserName"]?.ToString() == "Super Admin")
{
    <li style="background-color: #2C3E50 !important;" id="batch-status">
        <a href="@Url.Action("Indexstatus", "Batch")">
            <i class="fa fa-list"></i> &nbsp;
            <span style="color:white">Batch Status</span>
            <span class="badge badge-light" id="batchCount">0</span>
        </a>
    </li>
}