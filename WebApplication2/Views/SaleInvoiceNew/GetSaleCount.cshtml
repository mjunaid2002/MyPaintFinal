@if (Session["CurrentUserName"]?.ToString() == "Super Admin")
{
    <li style="background-color: #2C3E50 !important;" id="batch-status">
        <a href="@Url.Action("Indexstatus", "SaleInvoiceNew")">
            <i class="fa fa-list"></i> &nbsp;
            <span style="color:white">Sale Requests</span>
            <span class="badge badge-light" id="SaleCount1">0</span>
        </a>
    </li>
}
